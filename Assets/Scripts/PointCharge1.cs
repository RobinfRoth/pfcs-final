using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PointCharge1 : MonoBehaviour
{
    private float dt;
    public float radius;
    public float charge = 10; // Coulomb

    // Start is called before the first frame update
    void Start()
    {
        dt = Time.deltaTime;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // physics simulation
    void FixedUpdate()
    {        
    
    }
}
